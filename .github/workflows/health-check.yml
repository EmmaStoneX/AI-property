name: Service Health Check

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Playwright
        run: |
          npm init -y > /dev/null 2>&1
          npm install playwright > /dev/null 2>&1
          npx playwright install chromium > /dev/null 2>&1

      - name: Check all services with real browser
        run: |
          node << 'SCRIPT'
          const { chromium } = require('playwright');

          const services = [
            "ai.zxvmax.com",
            "api.zxvmax.com",
            "chat.zxvmax.com",
            "deep-research.zxvmax.com",
            "dify.zxvmax.com",
            "img.zxvmax.com",
            "nano-kit.zxvmax.com",
            "netpulse.zxvmax.com",
            "ui-prompt.zxvmax.com",
            "waoo.zxvmax.com",
            "backend-ai.zxvmax.com",
            "claude.zxvmax.com",
            "grok.zxvmax.com",
            "grok2api.zxvmax.com",
            "kagi2api.zxvmax.com",
            "cpa.zxvmax.com"
          ];

          (async () => {
            const browser = await chromium.launch();
            const results = {};

            for (const domain of services) {
              let status = 'down';
              let httpCode = 0;
              try {
                const context = await browser.newContext();
                const page = await context.newPage();
                const response = await page.goto('https://' + domain, {
                  waitUntil: 'domcontentloaded',
                  timeout: 30000
                });
                httpCode = response ? response.status() : 0;
                // up = 页面成功加载且不是 5xx
                if (httpCode > 0 && httpCode < 500) {
                  status = 'up';
                }
                await context.close();
              } catch (e) {
                status = 'down';
                httpCode = 0;
              }
              results[domain] = { status, httpCode };
              console.log(domain + ' -> ' + httpCode + ' (' + status + ')');
            }

            await browser.close();

            const output = {
              lastChecked: new Date().toISOString(),
              services: results
            };

            require('fs').writeFileSync('status.json', JSON.stringify(output, null, 2));
            console.log('\nstatus.json written');
          })();
          SCRIPT

      - name: Commit and push status
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add status.json
          git diff --cached --quiet || (git commit -m "chore: update service health status" && git push)
